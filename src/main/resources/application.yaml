spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: wellingtonfabi@gmail.com
    password: vmba ykcj lrzf rbzp
    protocol: smtp
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  thymeleaf:
    enable: true

envio:
  email:
    remetente: wellingtonfabi@gmail.com
    nomeRemetente: 'Javanauta'

server:
  port: 8082